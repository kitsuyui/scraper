version: 2
jobs:
  test:
    working_directory: /go/src/github.com/kitsuyui/scraper
    docker:
      - image: golang:1.12.9-stretch@sha256:2deb7a059e5b588f38e0330f92fd3f966d9d0f571804a34d61a0d41c47bf39a4
    steps:
      - checkout
      - setup_remote_docker
      - run:
          command: |
            ./bin/test.sh
            ./bin/coverage.sh
            bash <(curl -s https://codecov.io/bash)

  release:
    working_directory: /go/src/github.com/kitsuyui/scraper
    docker:
      - image: golang:1.12.9-stretch@sha256:2deb7a059e5b588f38e0330f92fd3f966d9d0f571804a34d61a0d41c47bf39a4
    steps:
      - checkout
      - setup_remote_docker
      - run:
          command: |
            go get github.com/mitchellh/gox
            go get github.com/tcnksm/ghr
            BUILD_VERSION="$CIRCLE_TAG" ./bin/build.sh
            mkdir /tmp/releases
            mv ./build/scraper_* /tmp/releases
            ghr -u "$CIRCLE_PROJECT_USERNAME" "$CIRCLE_TAG" /tmp/releases
      - store_artifacts:
          path: /tmp/artifacts

workflows:
  version: 2
  build_and_release:
    jobs:
      - test:
          filters:
            tags:
              only: /.*/
      - release:
          requires:
            - test
          filters:
            branches:
              ignore: /.*/
            tags:
              only: /^v\d+\.\d+.\d+$/
